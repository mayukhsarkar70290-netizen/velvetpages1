# Velvet Pages 1

A premium React + Firebase art gallery app.

## Setup
1. Run `npm install`
2. Create `.env` file:
```
VITE_FIREBASE_CONFIG={"apiKey":"..."}
VITE_APP_ID="velvet-pages-1"
```
3. Replace `src/App.jsx` with your full premium code from ChatGPT canvas.
4. Run `npm run dev`
